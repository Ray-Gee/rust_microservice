query getPageByTitle($title: String!) {
    pageByTitle(title: $title) {
        id
        title
        bodyHtml
        source
        createTime
        updateTime
        hatenaStars {
            id
            quote
            color
        }
    }
}